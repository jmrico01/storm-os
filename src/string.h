#ifndef STRING_H
#define STRING_H

#include "types.h"

int	StrCmpN(const char* str1, const char* str2, uint32 n);
int	StrLenN(const char* str, uint32 size);
/*char* StrCopyN(char *dst, const char *src, uint32 size);
void StrFlipN(char *s, uint32 size);*/

#endif
